/**
 * Centralized URL route constants
 * This file is now a wrapper around the shared constants in the convex folder,
 * making them accessible to both frontend and backend.
 */

export { ROUTE_PATTERNS } from "@convex/shared/routes";
