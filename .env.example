# Cascade Environment Variables Example
# Copy this file to .env.local and fill in your values

# ============================================================
# Email Configuration
# ============================================================
# Email provider to use: "resend" (default) or "sendpulse"
# Leave blank or omit to use Resend (default)
EMAIL_PROVIDER=

# Application URL (for links in emails)
APP_URL=http://localhost:5173

# ============================================================
# Resend (Default)
# ============================================================
# Get API key from: https://resend.com/api-keys
# Free tier: 3,000 emails/month
RESEND_API_KEY=re_xxxxxxxxxxxxx

# From email address (must be verified domain)
# Default: "Cascade <notifications@cascade.app>"
RESEND_FROM_EMAIL=Cascade <notifications@yourdomain.com>

# ============================================================
# Analytics (Optional)
# ============================================================
# PostHog analytics key
VITE_PUBLIC_POSTHOG_KEY=your_posthog_key
VITE_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ============================================================
# SendPulse (Alternative)
# ============================================================
# Set EMAIL_PROVIDER=sendpulse to use SendPulse instead
# Free tier: 15,000 emails/month
# Get credentials from: https://sendpulse.com/integrations/api
SENDPULSE_ID=your_sendpulse_id
SENDPULSE_SECRET=your_sendpulse_secret
SENDPULSE_FROM_EMAIL=Cascade <notifications@yourdomain.com>

# ============================================================
# SendGrid (Alternative - for Production)
# ============================================================
# Set EMAIL_PROVIDER=sendgrid to use SendGrid
# SENDGRID_API_KEY=your_api_key
# SENDGRID_FROM_EMAIL=Cascade <notifications@yourdomain.com>

# ============================================================
# Google Calendar Integration (Optional)
# ============================================================
# OAuth credentials for Google Calendar sync
# Get these from: https://console.cloud.google.com/apis/credentials
#
# Steps to set up:
# 1. Go to https://console.cloud.google.com/
# 2. Create or select a project
# 3. Enable "Google Calendar API"
# 4. Go to Credentials → Create Credentials → OAuth 2.0 Client ID
# 5. Add authorized redirect URI: http://localhost:5173/google/callback (dev)
#                              or https://yourdomain.com/google/callback (prod)
# 6. Copy Client ID and Client Secret below
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Site URL for OAuth callback (defaults to http://localhost:5173 in dev)
SITE_URL=http://localhost:5173

# ============================================================
# Convex
# ============================================================
# Convex deployment URL (auto-generated during convex dev)
# Don't set this manually - it's in .env.local after running: pnpm convex dev
# CONVEX_DEPLOYMENT=

# Convex Deploy Key (REQUIRED for Vercel/production deployments)
# Get this from: https://dashboard.convex.dev/d/peaceful-salmon-964
# Navigate to: Settings → Deploy Keys → Generate Deploy Key
# Add this to Vercel: Settings → Environment Variables → CONVEX_DEPLOY_KEY
CONVEX_DEPLOY_KEY=your_convex_deploy_key_here
